on:
  # Auto triggers disabled: this workflow now only runs manually if ever needed.
  workflow_dispatch:

jobs:
  build:
    if: false  # Fully disabled while repo is planning-only (plain markdown, no Gemfile).
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Skip notice
        run: echo Jekyll-workflow-disabled-remove-if-false-to-enable