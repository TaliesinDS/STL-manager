sqlalchemy>=2.0

alembic>=1.11

# For frequency-based filtering of common words in OC name detection
wordfreq>=3.0.1

# Note: the Python standard library includes the `sqlite3` module which is
# sufficient for the project's SQLite usage in most environments. The
# `pysqlite3-binary` wheel is optional and is known to be unavailable for
# some Windows/Python combinations; install it only if you specifically need
# an external SQLite driver. Example (manual):
#   pip install pysqlite3-binary

# YAML round-trip parser/emitter used by scripts/check_codex_duplicates.py for
# preserving comments and enforcing flow-style list formatting
ruamel.yaml>=0.18.0
Unidecode>=1.3
